{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/cpu-emul",
            "args": ["-s", "examples/fib_loop.pasm", "-e", "out.user.bin"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "runInTerminal": true
        }


    ]
}
